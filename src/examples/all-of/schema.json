{
    "type": "object",
    "required": ["allOf_required"],
    "properties": {
        "allOf": {
            "title": "AllOf",
            "description": "An example of an optional allOf construction",
            "allOf": [
                {
                    "$ref": "#/$defs/address"
                },
                {
                    "$ref": "#/$defs/user"
                }
            ]
        },
        "allOf_required": {
            "title": "AllOf (required)",
            "description": "An example of a required allOf construction",
            "allOf": [
                {
                    "$ref": "#/$defs/address"
                },
                {
                    "$ref": "#/$defs/user"
                }
            ]
        },
        "allOf_with_data": {
            "title": "AllOf with prefilled data",
            "description": "An example of a allOf construction with prefilled data",
            "allOf": [
                {
                    "$ref": "#/$defs/address"
                },
                {
                    "$ref": "#/$defs/user"
                }
            ]
        },
        "allOf_readonly": {
            "title": "AllOf readonly",
            "description": "An example of a readonly allOf construction with readonly option set in the UI schema",
            "allOf": [
                {
                    "$ref": "#/$defs/address"
                },
                {
                    "$ref": "#/$defs/user"
                }
            ]
        },
        "allOf_allow_empty": {
            "title": "AllOf allow empty",
            "description": "An example of a allOf construction with allowEmpty option set in the UI schema. If allowEmpty is true, the parent object is not unset when all items are removed.",
            "allOf": [
                {
                    "$ref": "#/$defs/address"
                },
                {
                    "$ref": "#/$defs/user"
                }
            ]
        },
        "allOf_tabs": {
            "title": "AllOf tabs",
            "description": "An example of a allOf construction with tabs option set in the UI schema",
            "allOf": [
                {
                    "$ref": "#/$defs/address"
                },
                {
                    "$ref": "#/$defs/user"
                }
            ]
        },
        "allOf_accordion": {
            "title": "AllOf accordion",
            "description": "An example of a allOf construction with accordion option set in the UI schema",
            "allOf": [
                {
                    "$ref": "#/$defs/address"
                },
                {
                    "$ref": "#/$defs/user"
                }
            ]
        }   
    },
    "$defs": {
        "address": {
            "type": "object",
            "title": "Address",
            "properties": {
                "street_address": {
                    "type": "string"
                },
                "city": {
                    "type": "string"
                },
                "state": {
                    "type": "string"
                }
            },
            "required": ["street_address", "city", "state"]
        },
        "user": {
            "type": "object",
            "title": "User",
            "properties": {
                "name": {
                    "type": "string"
                },
                "mail": {
                    "type": "string"
                }
            },
            "required": ["name", "mail"]
        }
    }
}

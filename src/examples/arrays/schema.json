{
    "type": "object",
    "required": ["array_required", "array_required_min1"],
    "properties": {
        "array_of_strings": {
            "title": "Array of strings",
            "description": "An array of primitive strings. If all items are removed, the array itself should be removed too",
            "type": "array",
            "items": {
                "type": "string",
                "title": "String"
            }
        },
        "array_required": {
            "title": "Array required",
            "description": "An array that is required, without minimal number of items. If all items are removed, an empty array should be kept",
            "type": "array",
            "items": {
                "type": "string",
                "title": "String"
            }
        },
        "array_required_min1": {
            "title": "Array required with minimum 1 item",
            "description": "An array that is required, with a minimum of 1 item. You can still remove all items, but an empty array should be kept",
            "type": "array",
            "minItems": 1,
            "items": {
                "type": "string",
                "title": "String"
            }
        },
        "array_with_allowEmptyArrays": {
            "title": "Array required with allowEmptyArrays",
            "description": "An array that is not required. Using the option.allowEmptyArrays, an empty array should be kept if all items are removed",
            "type": "array",
            "items": {
                "type": "string",
                "title": "String"
            }
        },
        "array_with_minimum_items": {
            "title": "Array with minimum items",
            "description": "An array with a minimum number of items",
            "type": "array",
            "minItems": 2,
            "items": {
                "type": "string",
                "title": "String"
            }
        },
        "array_with_unique_items": {
            "title": "Array with unique items",
            "description": "An array with unique items",
            "type": "array",
            "uniqueItems": true,
            "items": {
                "type": "string",
                "title": "String"
            }
        },
        "array_with_complex_items": {
            "title": "Array with complex items",
            "description": "An array with complex items",
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "string_field": {
                        "title": "String field",
                        "description": "A required text field between 1 and 10 characters",
                        "type": "string",
                        "minLength": 1,
                        "maxLength": 10
                    },
                    "number_field": {
                        "title": "Number field",
                        "description": "A number field",
                        "type": "number"
                    },
                    "boolean_field": {
                        "title": "Boolean field",
                        "description": "A boolean field, rendered as a checkbox",
                        "type": "boolean"
                    }
                }
            }
        },
        "array_readonly": {
            "title": "Array with readonly items",
            "description": "An array with readonly items",
            "type": "array",
            "items": {
                "type": "string",
                "title": "String"
            }
        },
        "array_sortable": {
            "title": "Array with sortable items",
            "description": "An array with sortable items",
            "type": "array",
            "items": {
                "type": "string",
                "title": "String"
            }
        }
    }
}
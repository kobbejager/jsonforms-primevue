{
    "type": "object",
    "required": ["enum_array_required"],
    "properties": {
        "enum_array": {
            "title": "Enum array (unique selections)",
            "description": "Array of enum values rendered using the 'variant' option (default: checkbox)",
            "type": "array",
            "uniqueItems": true,
            "items": {
                "type": "string",
                "enum": ["Option 1", "Option 2", "Option 3"]
            }
        },
        "enum_array_horizontal": {
            "title": "Enum array (horizontal)",
            "description": "Same as checkbox but laid out horizontally (options.horizontal = true)",
            "type": "array",
            "uniqueItems": true,
            "items": {
                "type": "string",
                "enum": ["Option 1", "Option 2", "Option 3", "Option 4", "Option 5", "Option 6"]
            }
        },
        "enum_array_toggle": {
            "title": "Enum array (ToggleSwitch)",
            "description": "Use options.variant = 'toggleswitch' to render ToggleSwitches",
            "type": "array",
            "uniqueItems": true,
            "items": {
                "type": "string",
                "enum": ["Option 1", "Option 2", "Option 3"]
            }
        },
        "enum_array_selectbutton": {
            "title": "Enum array (SelectButton)",
            "description": "Use options.variant = 'selectbutton' to render a PrimeVue SelectButton (multiple)",
            "type": "array",
            "uniqueItems": true,
            "items": {
                "type": "string",
                "enum": ["Option 1", "Option 2", "Option 3", "Option 4"]
            }
        },
        "enum_array_multiselect": {
            "title": "Enum array (MultiSelect)",
            "description": "Use options.variant = 'multiselect' to render a PrimeVue MultiSelect with chips. Particularly useful for long lists of short options.",
            "type": "array",
            "uniqueItems": true,
            "items": {
                "type": "string",
                "enum": ["Albania", "Andorra", "Austria", "Belgium", "Bulgaria", "Croatia", "Cyprus", "Czech Republic", "Denmark", "Estonia", "Finland", "France", "Germany", "Greece", "Hungary", "Iceland", "Ireland", "Italy", "Latvia", "Liechtenstein", "Lithuania", "Luxembourg", "Malta", "Moldova", "Monaco", "Montenegro", "Netherlands", "Norway", "Poland", "Portugal", "Romania", "San Marino", "Serbia", "Slovakia", "Slovenia", "Spain", "Sweden", "Switzerland", "Ukraine", "United Kingdom"]
            }
        },
        "enum_array_override": {
            "title": "Enum array (with overridden options)",
            "description": "Use options.enumValues to override the options displayed in the MultiSelect. Note that the enum values are still validated against the original enum values. This implies that the overridden options must be a subset of the original options.",
            "type": "array",
            "uniqueItems": true,
            "items": {
                "type": "string",
                "enum": ["Albania", "Andorra", "Austria", "Belgium", "Bulgaria", "Croatia", "Cyprus", "Czech Republic", "Denmark", "Estonia", "Finland", "France", "Germany", "Greece", "Hungary", "Iceland", "Ireland", "Italy", "Latvia", "Liechtenstein", "Lithuania", "Luxembourg", "Malta", "Moldova", "Monaco", "Montenegro", "Netherlands", "Norway", "Poland", "Portugal", "Romania", "San Marino", "Serbia", "Slovakia", "Slovenia", "Spain", "Sweden", "Switzerland", "Ukraine", "United Kingdom"]
            }
        },
        "enum_array_with_labels": {
            "title": "Enum array (with labels)",
            "description": "Use options.enumLabels to override the labels displayed in the MultiSelect. The values are still those defined in the JSON Schema enum or the options.enumValues.",
            "type": "array",
            "uniqueItems": true,
            "items": {
                "type": "string",
                "enum": ["BE", "FR", "DE", "NL"]
            }
        },
        "enum_array_required": {
            "title": "Enum array (required)",
            "description": "Required enum array",
            "type": "array",
            "uniqueItems": true,
            "items": {
                "type": "string",
                "enum": ["Option 1", "Option 2", "Option 3"]
            }
        },
        "enum_array_default": {
            "title": "Enum array (with defaults)",
            "description": "Enum array with default values",
            "type": "array",
            "uniqueItems": true,
            "items": {
                "type": "string",
                "enum": ["Option 1", "Option 2", "Option 3"]
            },
            "default": ["Option 1", "Option 3"]
        },
        "enum_array_readonly": {
            "title": "Enum array (readonly)",
            "description": "Enum array with default values in readonly mode (defined in the UI Schema)",
            "type": "array",
            "uniqueItems": true,
            "items": {
                "type": "string",
                "enum": ["Option 1", "Option 2", "Option 3"]
            },
            "default": ["Option 1", "Option 3"]
        }
    }
}



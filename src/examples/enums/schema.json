{
    "type": "object",
    "required": [],
    "properties": {
        "enum_field": {
            "title": "Enum field",
            "description": "An enum field",
            "type": "string",
            "enum": ["Option 1", "Option 2", "Option 3"]
        },
        "enum_field_override": {
            "title": "Enum field with overridden options",
            "description": "An enum field with overridden options. Note that the enum values are still validated against the original enum values. This implies that the overridden options must be a subset of the original options.",
            "type": "string",
            "enum": ["Option 1", "Option 2", "Option 3", "Option 4"]
        },
        "enum_field_with_default": {
            "title": "Enum field with default value",
            "description": "An enum field with a default value",
            "type": "string",
            "enum": ["Option 1", "Option 2", "Option 3"],
            "default": "Option 1"
        },
        "enum_field_readonly": {
            "title": "Enum field in readonly mode (defined in the UI Schema)",
            "description": "An enum field with a default value in readonly mode (defined in the UI Schema)",
            "type": "string",
            "enum": ["Option 1", "Option 2", "Option 3"],
            "default": "Option 1"
        },
        "oneof_enum": {
            "title": "Enum field with a oneOf construct",
            "description": "An enum field with a oneOf construct",
            "type": "string",
            "oneOf": [
                {
                    "const": "foo",
                    "title": "Foo"
                },
                {
                    "const": "bar",
                    "title": "Bar"
                },
                {
                    "const": "foobar",
                    "title": "FooBar"
                }
            ]
        },
        "enum_numbers": {
            "title": "Enum of numbers",
            "description": "Numeric enum values",
            "type": "number",
            "enum": [0, 1, 2, 3]
        },
        "enum_booleans": {
            "title": "Enum of booleans",
            "description": "Boolean enum via oneOf with titles",
            "type": "boolean",
            "oneOf": [
                { "const": true, "title": "True" },
                { "const": false, "title": "False" }
            ]
        },
        "enum_mixed_string_numbers": {
            "title": "Enum mixed string & number",
            "description": "Mixed types in enum",
            "type": ["string", "number"],
            "enum": ["one", 2, "three", 4]
        },
        "enum_radio": {
            "title": "Enum as radios",
            "description": "Render as radio buttons",
            "type": "string",
            "enum": ["Alpha", "Beta", "Gamma"]
        },
        "enum_radio_horizontal": {
            "title": "Enum as radios (horizontal)",
            "description": "Radio buttons laid out horizontally",
            "type": "string",
            "enum": ["One", "Two", "Three", "Four"]
        },
        "enum_selectbutton": {
            "title": "Enum as selectbutton",
            "description": "Render as SelectButton",
            "type": "string",
            "enum": ["Red", "Green", "Blue"]
        }
    }
}
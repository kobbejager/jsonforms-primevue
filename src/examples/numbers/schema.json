{
    "type": "object",
    "properties": {
        "number_field": {
            "title": "Number field",
            "description": "A number field restricted to values between 0 and 1",
            "type": "number",
            "minimum": 0,
            "maximum": 1
        }
    }
}